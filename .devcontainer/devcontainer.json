{
  "name": "C# (.NET)",
  "build": { "dockerfile": "Dockerfile" },
  "runArgs": ["--env-file", "${localWorkspaceFolder}/.devcontainer/.env"],
  "mounts": [
    "source=${localEnv:HOME}/.steam/steam/steamapps/common/Monster Train 2/BepInEx/plugins,target=/home/vscode/mt2-plugins,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.steam/steam/steamapps/common/Monster Train 2,target=/home/vscode/mt2-game,type=bind,consistency=cached,readonly",
    "source=${localEnv:HOME}/mt2-decompiled,target=/home/vscode/mt2-decompiled,type=bind,consistency=cached",
    "source=forgeconfirmation-claude-home,target=/home/vscode/.claude,type=volume"
  ],
  "postCreateCommand": "git lfs uninstall || true && rm -f .git/hooks/pre-push && dotnet nuget update source --username $GITHUB_USER --password $GH_AUTH_TOKEN monster-train-packages --store-password-in-clear-text && dotnet restore && curl -sSL https://dot.net/v1/dotnet-install.sh | bash -s -- --runtime dotnet --channel 8.0 --install-dir /usr/share/dotnet && (dotnet tool install -g ilspycmd --version 9.1.0.7988 || dotnet tool update -g ilspycmd --version 9.1.0.7988)",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-dotnettools.csdevkit",
        "Anthropic.claude-code"
      ],
      "settings": {
        "dotnet.defaultSolution": "**/*.sln",
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {}
  },
  "remoteUser": "vscode"
}
